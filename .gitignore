.venv/
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
runs/
# Generated datasets live in the Hugging Face bucket (too big for GitHub's 100 MB
# limit); pull them with `aczero dataset download`.
data/generated/
# Dataset summaries are derived artifacts, regenerated on every `grow` run.
data/summaries/
# Benchmark catalogs are a pure function of their two bounds, regenerated on
# demand by `aczero benchmark create` (the 48/7 catalog is ~14k presentations).
data/benchmarks/
