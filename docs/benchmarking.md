# Benchmarking

Benchmark reports must compare methods under explicit budgets: environment transitions, node expansions, model evaluations, wall-clock time, and memory where available. A result is solved only when the emitted certificate verifies.
