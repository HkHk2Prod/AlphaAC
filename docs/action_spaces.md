# Action Spaces

The strict ordinary action space contains only `AC1`, `AC2`, and generator-conjugation `AC3`. Rank `n` has `n(n-1) + n + 2n^2 = 3n^2` actions.

Macros, stable moves, swaps, and substitutions are not silently mixed into ordinary experiments. A macro must expand to strict primitive moves before certificate emission.
