# Silence the debugpy "frozen modules" breakpoint warning emitted when the
# Jupyter kernel / debugger attaches under Python 3.11+. Auto-loaded by the
# VSCode Python & Jupyter extensions (see python.envFile in .vscode/settings.json).
PYDEVD_DISABLE_FILE_VALIDATION=1
