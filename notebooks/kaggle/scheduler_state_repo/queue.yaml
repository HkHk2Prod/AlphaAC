version: 1
limits:
  max_total_active: 5
  max_cpu_active: 5
  max_gpu_active: 1
  max_launches_per_tick: 5
  stale_heartbeat_minutes: 180
tasks:
# Closest-first generation: expands outward from the trivial group by the inverses of the
# strict-ac moves, breadth-first, so every distance it writes is a proven optimum and each
# shell is complete before the next begins. It emits the annotation file itself, which is
# why no annotation task follows it. Resumes from the bucket: the groups file is the queue.
- id: ball-main
  mode: ball
  notebook_slug: hkhk2prod/alphaac-ball
  notebook_dir: notebooks/kaggle
  active: true
  remaining_runs: null
  accelerator: cpu
  priority: 100
  max_runtime_minutes: 705
  stop_after_current_iteration: false
  config:
    rank: 2
    moveset: strict-ac
    target: 100000000
    # Each checkpoint rewrites both documents, so keep them rare as the ball grows.
    checkpoint_every: 250000
    workers: 0
    output: /kaggle/working/ball_rank2.groups.json
# The length-first grow and the annotation pass that gave it distances. Retired in favour
# of `ball-main`: its distances were only ever upper bounds (a shortest path can run
# through a group it never expanded) and under strict-ac only 59% of its groups could
# reach the origin at all. Left here, inactive, because the grown dataset is still what
# the universal move set and the descent labels are built from.
- id: generation-main
  mode: generation
  notebook_slug: hkhk2prod/alphaac-generation
  notebook_dir: notebooks/kaggle
  active: false
  remaining_runs: null
  accelerator: cpu
  priority: 100
  max_runtime_minutes: 705
  stop_after_current_iteration: false
  config:
    rank: 2
    target: 100000000
    total_length_cap: 0
    workers: 0
    output: /kaggle/working/train_rank2.groups.json
- id: annotation-main
  mode: annotation
  notebook_slug: hkhk2prod/alphaac-annotation
  notebook_dir: notebooks/kaggle
  active: false
  remaining_runs: null
  accelerator: cpu
  priority: 90
  max_runtime_minutes: 705
  stop_after_current_iteration: false
  config:
    moveset: strict-ac
    max_depth: 0
    workers: 0
- id: train-alphazero-strict-navigation
  mode: training
  notebook_slug: hkhk2prod/alphaac-train-alphazero
  notebook_dir: notebooks/kaggle
  active: true
  remaining_runs: null
  accelerator: cpu
  priority: 50
  max_runtime_minutes: 705
  stop_after_current_iteration: false
  config:
    rank: 2
    agent: alphazero
    model: residual_mlp
    moveset: strict-ac
    total_length_cap: 128
    seed: 0
    dataset:
      bucket: HkHk2Prod/alphaac-data
      max_difficulty: null
    training:
      reward_mode: navigation
      gamma: 0.99
      iterations: 100000000
      episodes_per_iteration: 24
      optimizer_updates: 8
      batch_size: 64
      replay_capacity: 4096
      learning_rate: 0.02
      value_loss_weight: 1.0
      mcts_simulations: 128
      c_puct: 1.5
      checkpoint_every: 1
      workers: 0
- id: train-ppo-strict-navigation
  mode: training
  notebook_slug: hkhk2prod/alphaac-train-ppo
  notebook_dir: notebooks/kaggle
  active: true
  remaining_runs: null
  accelerator: cpu
  priority: 50
  max_runtime_minutes: 705
  stop_after_current_iteration: false
  config:
    rank: 2
    agent: ppo
    model: residual_mlp
    moveset: strict-ac
    total_length_cap: 128
    seed: 0
    dataset:
      bucket: HkHk2Prod/alphaac-data
      max_difficulty: null
    training:
      reward_mode: navigation
      gamma: 0.99
      iterations: 100000000
      episodes_per_iteration: 32
      batch_size: 64
      ppo_epochs: 4
      ppo_lambda: 0.95
      ppo_clip: 0.2
      entropy_coef: 0.01
      value_loss_weight: 0.5
      learning_rate: 0.02
      checkpoint_every: 1
      workers: 0
- id: train-alphazero-strict-navigation-transformer
  mode: training
  notebook_slug: hkhk2prod/alphaac-train-alphazero-transformer
  notebook_dir: notebooks/kaggle
  active: true
  remaining_runs: null
  accelerator: cpu
  priority: 50
  max_runtime_minutes: 705
  stop_after_current_iteration: false
  config:
    rank: 2
    agent: alphazero
    model: transformer
    moveset: strict-ac
    total_length_cap: 128
    seed: 0
    dataset:
      bucket: HkHk2Prod/alphaac-data
      max_difficulty: null
    training:
      reward_mode: navigation
      gamma: 0.99
      iterations: 100000000
      episodes_per_iteration: 24
      optimizer_updates: 8
      batch_size: 64
      replay_capacity: 4096
      learning_rate: 0.02
      value_loss_weight: 1.0
      mcts_simulations: 128
      c_puct: 1.5
      checkpoint_every: 1
      workers: 0
- id: train-ppo-strict-navigation-transformer
  mode: training
  notebook_slug: hkhk2prod/alphaac-train-ppo-transformer
  notebook_dir: notebooks/kaggle
  active: true
  remaining_runs: null
  accelerator: cpu
  priority: 50
  max_runtime_minutes: 705
  stop_after_current_iteration: false
  config:
    rank: 2
    agent: ppo
    model: transformer
    moveset: strict-ac
    total_length_cap: 128
    seed: 0
    dataset:
      bucket: HkHk2Prod/alphaac-data
      max_difficulty: null
    training:
      reward_mode: navigation
      gamma: 0.99
      iterations: 100000000
      episodes_per_iteration: 32
      batch_size: 64
      ppo_epochs: 4
      ppo_lambda: 0.95
      ppo_clip: 0.2
      entropy_coef: 0.01
      value_loss_weight: 0.5
      learning_rate: 0.02
      checkpoint_every: 1
      workers: 0
- id: solve-greedy-strict
  mode: solve
  notebook_slug: hkhk2prod/alphaac-solve-greedy
  notebook_dir: notebooks/kaggle
  active: false
  remaining_runs: null
  accelerator: cpu
  priority: 40
  max_runtime_minutes: 705
  stop_after_current_iteration: false
  config:
    rank: 2
    agent: greedy
    dataset:
      bucket: HkHk2Prod/alphaac-data
